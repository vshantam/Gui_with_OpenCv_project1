The login Credentials:>
      
        username:->'root'
        password:->'toor'
        
        #Note:->application allows to enter the wrong credentials only 3 times after that application gets killed. 
